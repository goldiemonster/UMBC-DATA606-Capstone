# Raw USAspending contract archives

Run from the repository root:

```bash
python data/download_usaspending_contracts.py
```

The ZIP files downloaded into this directory are intentionally ignored by Git
because they are large authoritative source files that can be reproduced from
USAspending.gov.

`manifest.csv` is retained to document data provenance and checksums.
